const mongoose = require('mongoose')

/*const ShopShema2 = new mongoose.Schema({
    id:,
    title:String,
    orderCount:Array,
    positiveRate:,
    imgUrl:
})*/

module.exports = mongoose.model('shop',ShopShema2)